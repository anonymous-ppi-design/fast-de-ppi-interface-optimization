[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fastde"
version = "0.1.0"
description = "Anonymized FAST-DE demo package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
